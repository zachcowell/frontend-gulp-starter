'use strict';

// express server config options

var dist = 'dist';

module.exports = {
  user: {
    name: 'test',
    password: 'test'
  },
  dest: '/../' + dist,
  port: 3001,
  env: 'development'
};
